define(['marionette.main'],function(){

	return function(App){
		/* Module definition code starts here */
		App.module("login", function(MyModule, App, Backbone, Marionette, $, _){

		  console.log('Inside login!');

		  // If you require any module specific dependencies during code execution
		  // such as loading any plugins you can execute them inside a require block as follows
		  // var additionalPlugins = [];
		  // require(additionalPlugins,function(){
		  		// Plugin dependent code goes here
		  // });

		});
	}

});